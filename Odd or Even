#include <stdio.h>|

int main () {
	int a, b;
	  
	printf ("Enter your number: ", a);
	scanf ("%d", &a);	
	
	b = a % 2;
	
	if (a% 2 == 0) {
		printf ("%d is even", a);
	} 
	else {
		printf ("%d is odd", a) ;
	}
		return 0;
}
